# Vault Watcher Gate

## Scope and Cause

CI `35815843690` passed Windows, Linux, both macOS core jobs, and Nix, but exposed a Linux Runtime failure: Node's recursive watcher remained attached to the retired inode after atomic note replacement. Disk contents changed while `note-vault/change` stopped refreshing linked panes.

Replaced that watcher with exact `chokidar@5.0.0`. Retained nonpersistent operation, canonical path/generation checks, symlink exclusion, hidden-path filtering, and atomic saves. Initial document/tree/inspection reads wait cancellably for readiness. Disposal awaits current and retired watcher cleanup. A 50 ms stability window retains the final edit in a burst.

Independent review found and then accepted corrections for Windows root equivalence, nonpersistent native-error forwarding, and write-finish polling surviving close. The latter two use a narrow pnpm patch, registered identically in both workspace configurations and locks. The staged runtime resolves the patched copy, not the unrelated Chokidar used by pinned DSH. No pinned DSH or installed dependency was hand-edited.

## Verification

- `node --test tests/tocktutor-watcher.test.ts`: all three defects reproduced RED; final **4/4 PASS**. This packaged-runtime regression is included in the root OS matrix. It covers equivalent root spellings, save/in-place/atomic/later edits, real native-watcher error forwarding, pending-poll cancellation, and a late stat error after disposal.
- `node --test --test-name-pattern='watcher ' plugins/tocktutor/packages/tockbot-note-runtime/tests/loader-composition.test.ts`: **3/3 PASS**; nested edits, atomic replacements, boundary filtering, startup cancellation, vault switching, and disposal.
- `node --test plugins/tocktutor/packages/tockteam-tocktutor-workbench/tests/vault-events-runtime.test.ts`: **6/6 PASS** with real Cordis delivery.
- `node --test plugins/tocktutor/packages/tockteam-tocktutor-workbench/tests/*.test.ts`: **359/359 PASS**. Previously accepted 578 Vitest checks and menu/editor evidence remain unchanged.
- `node --test plugins/tocktutor/packages/tockbot-note-runtime/tests/*.test.ts`: **209 PASS, 1 environment-blocked**. Host-death inventory/cleanup requires a process-inspection spawn denied with `EPERM`; its temporary fixture was retained, not deleted without cleanup proof.
- `node --test tests/*.test.ts`: **1,372 PASS, 18 SKIP, 5 environment-blocked**. Two marketplace checks cannot apply the macOS sandbox; three process-inventory/cleanup checks receive `spawn EPERM`. No security check was weakened or skipped to obtain a green local result.
- Pinned pnpm 11.21.0 frozen installs for both workspaces, workspace build, root TypeScript/build, quick runtime staging, and build-manifest verification passed. Unchanged client bundles were restored after incidental rebuild path/order churn; changed runtime outputs were generated by TypeScript. Root dependency-refresh noise outside Chokidar/readdirp was excluded.
- `node scripts/tocktutor-build-manifest.mjs --check` and `git diff --check`: PASS.

## Desktop Proof

A fresh production Desktop session on the extended display saved `Editable.md` through Source Mode and the Save shortcut. Exact saved bytes were checked on disk. An external in-place edit, atomic replacement, and subsequent in-place edit each refreshed the visible editor to the corresponding content. Final screenshot: `source-dark.png`; evidence: `desktop-proof.json`.

Verified **1512 × 949 CSS pixels**, **2× scale**, **3024 × 1898 PNG pixels**, route `/tocktutor/Editable.md`, Source Mode, explicit built-in dark theme, and no active skin. No page or console errors were recorded. The owned app/Host/browser process tree was explicitly stopped with no remaining PIDs. The earlier selector-probing session was also stopped. Clipboard and native-open effects remained intercepted and are not certified.

## Review and Remaining Gates

Initial review run `7dede8b8-f96b-406f-b3fe-5934c7c6b2b4`; accepted correction review `118271af-8c53-4228-9129-0e1054df3573`. See `independent-review.md`; acceptance is bounded to the three findings and does not claim independent execution.

At this local evidence checkpoint, the new cross-platform/Runtime CI and refreshed Nix dependency hash are pending. Final CI disposition is tracked in Beads `tockteam-yoam.26` and the branch workflow. No local Nix, installed executable smoke, real OS association, clipboard mutation, or native-open certification is claimed. No PR creation or merge is authorized.
