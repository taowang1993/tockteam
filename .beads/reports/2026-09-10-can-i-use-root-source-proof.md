# Can I Use Root Source Proof

Issue: `tockteam-3l3.4.2` — implementation and application integration remain in progress.

## Execution Scope

This checkpoint connects the verified inert data to the **unchanged pinned root component** through finite first-party aliases in a disposable Node probe. It is not a Launcher, Electron, detail/action, or distribution admission claim.

The explicitly scoped test execution uses:

- Source archive: 3,246,080 bytes, SHA-256 `cd79b55c49f36836970b56d9f7ecef39f89a4855bb5d20aca5576299edb2837c`.
- Previously admitted Translate archive supplying its private React 19.0.0 runtime: SHA-256 `7a27b1a75d4ee978fab04281dd93e187a6c32fd1de5de1f01eb66ce7682ea3ac`.
- The separately admitted data snapshot from `695a7299`.
- First-party source-root validation, preference preparation and five closed build aliases. The emitted probe's only external imports are `react` and `react/jsx-runtime`.

No Browserslist/caniuse package implementation, updater, configuration resolver, workspace reader, detail rendering, source action callback, or native effect is exercised. The source root constructs React elements; this probe does **not** exercise the shared reconciler or the Launcher manager. A detached Node process is trusted code, not an OS sandbox.

## Implemented and Verified

- Host-only immutable membership exposes the already validated 649 canonical targets for exact-query preparation. The child receives only the selected targets, never that full membership collection.
- The Host preparation function retires prior authority before validating preferences or search, searches all 581 catalog entries, and materializes at most 64 feature identities, titles, statuses and support booleans.
- Root packets are bounded to 32 KiB. Context, revision, snapshot query, row shape/order, counts, status values and target arguments are validated. Expected context is supplied separately by the Host bootstrap, not reconstructed from an incoming packet.
- Feature tokens are opaque, frozen and instance-bound. Old aliases reject after replacement; changed queries/environments require a fresh source instance. Workspace and path aliases remain unavailable.
- Real source execution rendered the first 64 rows, found catalog index 500 after a search, rendered the exact CSS Grid status/support accessories, and rendered an empty result with count zero.
- A real regression showed that a changed snapshot environment could produce a fifth source view. The fix rejects the change with `SNAPSHOT_STALE` before any new view. This is distinct from the earlier synthetic contract checks.
- Missing/unsupported defaults and workspace selections do not become a successful fallback. Opera Mini-only selections intentionally omit support indicators, matching the unchanged source's filtering.
- The source archive and compiled probe are rehashed after execution; source/build files matched their recorded preflight hashes. The process group is forcibly cleaned and verified gone before deleting its disposable workspace. Final R5 probe PID/group: `93311`.

## Evidence and Failures

Evidence: `/private/tmp/tockteam-can-i-use-root-bridge-r1.XsioBP/`.

- `targets-red.txt`: missing Host membership output, followed by the minimal immutable output change.
- `command-red.txt`: failing bounded-root behavior before implementation.
- Source listing with a 2 MB guard failed before reading; the exact recorded archive size justified a fresh 3,246,080-byte bounded listing. The first attempt remains a failure.
- `source-execution-r1.txt`: virtual-module build resolution failed; no candidate code ran. Supplying the fixed first-party resolution directory repaired the build (`source-execution-r2.txt`).
- `source-environment-red-r3.txt`: genuine unexpected fifth view; `source-environment-green-r4.txt`: cause-specific rejection passed.
- `source-test-r5-preflight.sha256`, `contract-source-final-r5.txt`: final source execution plus unchanged first-party inputs and process-group cleanup.
- `adapter-final.txt`, `admission-boundaries.txt`, `typecheck-final.txt`, and `bridge-coverage.txt`: focused verification. Subsequent pure transport-bound coverage is retained separately; it does not repeat candidate execution.

Exact verification commands:

```sh
/opt/homebrew/opt/node@24/bin/node --test --test-isolation=none tests/trusted-raycast-can-i-use-data.test.mjs
PATH=/opt/homebrew/opt/node@24/bin:$PATH ./node_modules/.bin/tsx --test tests/trusted-raycast-can-i-use-{assets,preferences,actions,aliases,catalog,query,workspace,command,source}.test.ts
PATH=/opt/homebrew/opt/node@24/bin:$PATH ./node_modules/.bin/tsx --test --test-name-pattern='owning package|descriptor' tests/trusted-raycast-package-contract.test.ts tests/trusted-raycast-descriptors.test.ts
PATH=/opt/homebrew/opt/node@24/bin:$PATH ./node_modules/.bin/tsc --noEmit
```

## Remaining Boundaries

The production descriptor list still contains only Translate and Kaomoji. No production manager, child protocol, renderer, trust store, profile, packaged runtime, or Electron application was changed or launched. Both existing extension archives retain their exact pins.

Before closing `.4.2`, connect this source-root seam to the existing manager/child lifecycle, accept only the corresponding source projection, expose accurate counts in the UI, and verify stale/error/close behavior through that actual lifecycle. `.4.3` must add authenticated details/browser actions and preference lifecycle. `.4.4` retains final artifact/legal materialization, packaging and disposable Electron proof. Do not mistake this root probe for those gates.

## Recommended Evidence Recheck

On 2026-09-10, a fresh read of <https://www.raycast.com/store> explicitly showed **Featured** with Kaomoji Search, Can I Use and Mole. Its navigation remained **All Extensions**, **Recently Added**, and **Most Popular**. The retained Markdown capture SHA-256 is `294948f6aaff385f5a545aab78bd909d149efd984deda24f82488232e3caa05f` (`current-raycast-store.md` in the evidence directory).

Two Raycast-domain Exa searches did not yield authoritative Recommended membership or Featured-equivalence evidence. That is a retrieval limitation, not proof that such a collection does not exist. `tockteam-3l3.2` is now explicitly blocked on that evidence or an approved scope change. No private account inspection or app/input automation was used.
